# Data outputs (large / regenerable — share via Google Drive)
data/outputs/*
!data/outputs/.gitkeep
*.tif
*.tiff
*.zip

# Regenerable data downloaded/written by running the notebook locally
# (paths meant for Colab's /content/ resolve here instead)
M2_classification/*.geojson

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
env/

# Earth Engine / credentials
*.json.key
credentials
.config/earthengine/

# OS
.DS_Store
Thumbs.db
